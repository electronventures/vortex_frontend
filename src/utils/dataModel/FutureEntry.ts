class FutureEntry {
  futureEntryCount: number;
  futureEntryTotal: number;

  constructor({ futureEntryCount, futureEntryTotal }: FutureEntry) {
    this.futureEntryCount = futureEntryCount;
    this.futureEntryTotal = futureEntryTotal;
  }
}

export default FutureEntry;
